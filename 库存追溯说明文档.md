## 主要修改内容

### 1. 入库管理优化

#### 1.1 创建入库单

创建入库单页面的零件选择已改为下拉选择，数据来源于“零件管理”模块。

用户添加入库明细时，不再手动随意输入零件号，而是从已有零件中选择。选择零件后，系统会自动带出：

- 零件号
- 零件名称
- 包装容量
- 单位
- 供应商信息

这样可以减少录入错误，并保证入库单数据和基础数据一致。

#### 1.2 扫码入库

扫码入库逻辑继续使用看板二维码作为入库凭证。

入库成功后，系统会同步更新：

- 当前库存
- 看板状态
- 入库追溯记录

---

### 2. 出库管理优化

#### 2.1 新增出库管理模块

系统增加了独立的 WMS 出库管理模块，包含：

- 出库单列表
- 创建出库单
- 扫码出库
- 出库进度显示
- 出库单明细

出库单状态包括：

| 状态 | 说明 |
|---|---|
| pending | 待出库 |
| partial | 部分出库 |
| completed | 已完成 |

#### 2.2 出库单创建

创建出库单页面也改为从“零件管理”中选择零件。

选择零件后会自动带出包装容量，用于计算预期箱数。

#### 2.3 扫码出库

扫码出库流程已修复并完善。

现在系统支持两类看板：

| 看板类型 | 用途 |
|---|---|
| 库存看板 | 已入库、当前库存中存在的看板 |
| 出库看板 | 出库单打印出来的出库标签 |

出库时系统会校验：

- 出库单是否存在且未完成。
- 扫描看板是否属于当前出库单。
- 零件是否在出库单明细中。
- 该零件是否还有剩余出库数量。
- 当前库存是否足够。
- 是否符合 FIFO 先进先出规则。

出库成功后，系统会同步更新：

- 当前库存数量
- 看板状态
- 出库单已出库数量
- 出库单明细已出库数量
- 出库单状态
- 库存追溯记录

#### 2.4 FIFO 先进先出

出库扣库存时，系统会按照入库时间排序，优先扣减最早入库的库存。

如果用户扫描的是库存看板，系统会校验该看板是否为当前零件最早入库的可用库存。

如果用户扫描的是出库看板，系统会使用出库看板匹配出库单和零件，再按 FIFO 自动扣减真实库存。

#### 2.5 出库看板打印修复

之前出库单列表中打印看板时，会把已经完成出库的明细也打印出来，导致用户反复扫到“已完成出库”的零件，出库进度无法继续变化。

现在已修复：

- 已完成出库的明细不会再生成出库看板。
- 出库看板只显示剩余未出库数量。
- 后端会拦截已完成明细生成看板。
- 后端会拦截库存不足明细生成看板。

---

### 3. 库存追溯独立化

库存追溯已经从入库功能中独立出来，成为单独菜单。

库存追溯页面包含：

- 全部流水
- 入库历史
- 出库历史
- 当前库存
- 看板生命周期

这样入库和出库记录可以统一查看，不再只挂在入库模块下面。

#### 3.1 入库历史

显示所有入库操作记录，包括：

- 看板号
- 入库单号
- 零件号
- 零件名称
- 数量
- 库位
- 操作人
- 操作时间

#### 3.2 出库历史

显示所有出库操作记录，包括：

- 看板号
- 出库单号
- 零件号
- 零件名称
- 数量
- 库位
- 操作人
- 操作时间

#### 3.3 当前库存

显示当前仍在库的库存数据。

库存为 0 的数据不会继续作为可用库存参与出库。

#### 3.4 看板生命周期

可按看板号查询看板从生成、入库、出库到追溯记录的完整生命周期。

---

### 4. 基础数据模块完善

#### 4.1 供应商管理

修复了新增供应商失败的问题。

之前如果供应商被删除后再次使用相同供应商编码新增，会因为数据库唯一索引报错。

现在逻辑改为：

- 如果供应商编码不存在，正常新增。
- 如果供应商编码已存在但被软删除，则恢复并更新该供应商信息。
- 如果供应商编码已存在且启用，则提示重复。

#### 4.2 零件管理

零件管理中的数据已经被入库单和出库单页面引用。

入库、出库添加明细时，会从零件管理中选择零件，并自动带出包装容量等基础信息。

---

## 三、修复的主要 Bug

| 问题 | 修复结果 |
|---|---|
| 新增供应商时相同编码报唯一索引错误 | 已支持恢复软删除供应商 |
| 库存追溯重置按钮不生效 | 已修复日期范围和筛选条件重置 |
| 出库管理中仍保留出库历史 | 已移到库存追溯中统一查看 |
| 创建入库单添加零件只能手动输入 | 已改为从零件管理下拉选择 |
| 创建出库单添加零件只能手动输入 | 已改为从零件管理下拉选择 |
| 扫码出库没有传看板号 | 已提交真实扫描看板号 |
| 扫描出库单看板无法继续出库 | 已支持出库看板匹配订单并按 FIFO 扣库存 |
| 出库单进度不变化 | 已修复出库成功后订单和明细数量更新 |
| 已完成明细仍能打印出库看板 | 已禁止生成无效出库看板 |
| 无库存零件仍能生成出库看板 | 已禁止生成库存不足的出库看板 |

---

## 四、数据清理

为了后续重新测试，已经清空以下业务流水数据：

- 入库单
- 入库单明细
- 出库单
- 出库单明细
- 看板数据
- 当前库存
- 库存追溯记录

保留以下基础数据：

- 用户
- 供应商
- 零件
- 仓库
- 库位

清空后接口验证结果：

| 模块 | 当前数量 |
|---|---|
| 入库单 | 0 |
| 出库单 | 0 |
| 当前库存 | 0 |
| 库存追溯 | 0 |

---

## 五、验证情况

本次修改后已执行以下验证：

### 后端测试

```bash
mvnw -q test
```

验证结果：通过。

### 前端测试

```bash
npm test -- --run
```

验证结果：通过。

### 前端构建

```bash
npm run build
```

验证结果：构建通过。

说明：构建过程中仍存在项目原有的 Vite/Rolldown 警告，但不影响本次功能运行。

---

## 六、当前运行方式

项目已增加本地启动脚本：

```bash
scripts/start-local.ps1
```

启动后访问地址：

| 服务 | 地址 |
|---|---|
| 前端 | https://localhost:5174/ |
| 后端 | http://localhost:8081/api |
| MySQL | 127.0.0.1:3307 |

本地停止脚本：

```bash
scripts/stop-local.ps1
```

---

## 七、后续使用建议

测试完整出入库流程时，建议按下面顺序操作：

1. 在供应商管理中确认供应商存在。
2. 在零件管理中确认零件和包装容量正确。
3. 创建入库单。
4. 打印入库看板。
5. 扫码入库，形成当前库存。
6. 创建出库单。
7. 打印出库看板。
8. 扫码出库。
9. 在库存追溯中查看入库历史、出库历史、当前库存和看板生命周期。

注意：出库前必须先有对应零件的当前库存，否则系统会阻止生成出库看板或阻止出库。

